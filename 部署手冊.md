# Phase 3 部署手冊 — PWA 雲端同步 + Cloudflare Pages

完成後你就有一個**真正的 iPhone 主畫面 App**，任何裝置記帳都會即時同步到 Google Sheets。

預計 20 分鐘。

---

## 一、先確認你手上的東西

來自 Phase 2 部署：

- [ ] Web App URL（例：`https://script.google.com/macros/s/AKfy.../exec`）
- [ ] API_SECRET（64 字元亂數）

本資料夾 `Phase3_PWA雲端同步/` 內：

- [ ] `index.html`
- [ ] `service-worker.js`
- [ ] `manifest.json`
- [ ] `icon-192.png`
- [ ] `icon-512.png`

> ⚠️ 不要把 `webapp-url.local.txt` 或任何 `.local.*` 上傳！只上傳上面 5 個檔。

---

## 二、本機快速測試（5 分鐘）

部署到雲端之前先在電腦測過比較安心。

### 2.1 起一個本機 HTTP server

PWA 與 Service Worker 不支援 `file://` 協定，一定要用 HTTP。

```powershell
cd "C:\Users\Administrator\Desktop\自創的記帳總表\Phase3_PWA雲端同步"
python -m http.server 8080
```

### 2.2 用瀏覽器開啟

[http://localhost:8080](http://localhost:8080)

### 2.3 設定雲端同步

1. 點下面 tabbar 的「⚙ 設定」
2. 最上方「☁️ 雲端同步」section：
   - **Web App URL**：貼上 Phase 2 拿到的網址
   - **API Secret**：貼上 64 字元字串
3. 點「**測試連線**」→ 應該看到「✅ 連線成功」toast
4. 點「**儲存設定**」
5. 若本機有舊資料：會問是否上傳到雲端 → 看你需求選

### 2.4 驗證自動同步

1. 切到「＋ 記帳」分頁
2. 隨便記一筆支出 50 元
3. 切到 Header 的同步圖示應該短暫變橘色（同步中）然後變綠色 ✓
4. 開 Google Sheets「家庭記帳本」→ 明細分頁 → 應該看到那筆 50 元
5. 切回 PWA 編輯/刪除那筆 → Sheets 也應該跟著變

> 若同步圖示一直是紅色 ✕：點圖示會跳到設定頁顯示錯誤原因（多半是 URL 或 Secret 錯）

---

## 三、部署到 Cloudflare Pages（10 分鐘）

### 3.1 註冊 Cloudflare（如果還沒）

1. 開 [https://pages.cloudflare.com](https://pages.cloudflare.com)
2. 用 Email 註冊（**免綁信用卡**，免費方案夠用）
3. 驗證 Email

### 3.2 建立 Project

1. Cloudflare Dashboard → 左側 **Workers & Pages**
2. 點 **Create application** → 上方分頁切到 **Pages** → **Upload assets**
3. 填 Project name：`ledger`（會變成 `ledger-xxx.pages.dev`，xxx 是隨機字串）
4. 點 **Create project**

### 3.3 上傳檔案

兩種方式擇一：

**A. 拖曳資料夾**
直接把 `Phase3_PWA雲端同步` 整個資料夾拖進 Cloudflare 的上傳框。

**B. 打包成 zip**
```powershell
cd "C:\Users\Administrator\Desktop\自創的記帳總表"
Compress-Archive -Path "Phase3_PWA雲端同步\*" -DestinationPath "ledger.zip" -Force
```
拖 `ledger.zip` 到 Cloudflare。

> ⚠️ **不要**包含 `webapp-url.local.txt`、`.gitignore`、`.secret.*` 之類的檔案。

### 3.4 確認 5 個檔都在

上傳完畢，Cloudflare 會列出檔案：
- `index.html`
- `manifest.json`
- `service-worker.js`
- `icon-192.png`
- `icon-512.png`

### 3.5 部署

點 **Deploy site**，等 30 秒。

### 3.6 取得網址

部署完顯示：`https://ledger-xxx.pages.dev`

開啟確認介面正常。

---

## 四、iPhone 加到主畫面（3 分鐘）

1. iPhone Safari 打開 `https://ledger-xxx.pages.dev`
2. 點下方分享按鈕（方框向上的圖示）
3. 往下滑找到「**加入主畫面**」
4. 命名「記帳」（或你喜歡的）→ 加入
5. 回到主畫面 → 點剛加的圖示 → 全螢幕 PWA

### 設定雲端同步（iPhone 端）

第一次在 iPhone 開記帳：
1. 設定 → ☁️ 雲端同步
2. 貼上 Web App URL + API Secret
3. 測試連線 → 儲存設定
4. **不要**上傳本地資料（iPhone 是空的），按取消
5. 改點「**從雲端還原**」→ 輸入「還原」→ 拉回所有資料

之後 iPhone 跟電腦會雙向同步（透過 Sheets 當中央）。

---

## 五、改版更新流程

之後你或我改了 `index.html`：

1. 回到 Cloudflare Pages → 該 project
2. 點 **Create deployment** → 上傳新檔案
3. 30 秒部署完成
4. iPhone 上的 PWA 重新打開會自動抓最新版（Service Worker 換 cache version）

> 改版要記得把 `service-worker.js` 內的 `CACHE_NAME` 加版本號（例如 `ledger-v1.3`），否則舊版會卡在快取。

---

## 六、疑難排解

### 同步圖示一直紅色
- 點圖示跳到設定 → 看「上次失敗」原因
- 多半是 URL 或 Secret 改了沒同步
- `unauthorized` → Secret 不對
- 連線錯誤 → 確認電腦/手機有網路

### Sheets 出現重複資料
- 多裝置短時間多次儲存可能造成
- 解法：保留一筆，刪掉其他（PWA 端會自動同步到 Sheets）

### 加到主畫面後沒有全螢幕
- 確認 manifest.json 內 `display: standalone`
- iPhone 必須**從主畫面圖示**打開才是全螢幕；從 Safari 開不會

### 換手機後資料沒了
- 新手機加到主畫面 → 設定雲端同步 → **從雲端還原**
- 不要先記任何東西，否則會跟雲端混合

### 想清掉所有本地資料重來
- DevTools（電腦）→ Application → Local Storage → 移除 `ledger_v1` 跟 `ledger_sync_v1`
- iPhone：設定 → Safari → 清除網站資料

---

## 七、隱私重點

- **secret 只存在你裝置的 localStorage + Apps Script 的 Properties** — 不在原始碼裡
- Cloudflare 看到的是公開的 PWA 介面，**看不到你的資料**（資料是 PWA → 直連 Apps Script → Sheets）
- 任何人開你的 Cloudflare URL 只會看到一個**沒設定 secret 的空 PWA**，記不進任何雲端
- 想完全停用雲端：設定頁清掉 URL + Secret → 儲存（變成單機模式）

---

## 八、下一步

→ **Phase 4**：在 Sheets 內加月度分析儀表板（每日彙總、類別佔比、轉帳明細圖表）
