# Phase 3 驗收測試清單

跑完部署手冊 §二 + §三 後，依序執行下面測試。

---

## A. 設定流程（5 項）

- [ ] 設定頁最上方看得到「☁️ 雲端同步」section
- [ ] 沒填 URL/Secret 時點測試連線 → toast「請先填 URL 與 Secret」
- [ ] 填正確 URL+Secret → 測試連線 → toast「✅ 連線成功」
- [ ] 填錯誤 Secret → 測試連線 → toast「❌ unauthorized」
- [ ] 儲存成功後 Header 同步 badge 變綠色 ✓

---

## B. 自動同步（5 項）

> 先確認 Sheets 是空的（沒測試殘留）

- [ ] 新增一筆支出 NT$ 100、飲食、外食 → 5 秒內 Sheets「明細」分頁出現該列
- [ ] Sheets 該列 J 欄（帳戶）顯示中文「現金」（不是 `acc_cash`）
- [ ] 編輯該筆改成 NT$ 120 → Sheets 同步更新
- [ ] 刪除該筆 → Sheets 該列消失（往上補位）
- [ ] 新增一筆轉帳 NT$ 1500 從現金到夢想帳戶 → Sheets 出現轉帳列，K=現金、L=夢想帳戶

---

## C. 離線重試（3 項）

- [ ] DevTools Network → 切 Offline → 新增 2 筆紀錄 → Header badge 變黃色「⚠ 2」
- [ ] DevTools Network → 切回 Online → 數秒內 badge 變綠 ✓，Sheets 多 2 筆
- [ ] 切 Offline 編輯一筆已上傳的紀錄 → 上線後 Sheets 同步更新

---

## D. 首次全量上傳（3 項）

> 步驟：先把 Sheets 清空 + 清掉本機同步設定 + 本機留 N 筆資料

- [ ] 重新填入 URL/Secret 儲存 → 跳出「本機 N 筆紀錄，雲端為空。要全部上傳嗎？」
- [ ] 確認 → toast「開始上傳…」→ 完成後 toast 顯示成功筆數
- [ ] Sheets 出現 N 筆對應紀錄（包含轉帳列正確）

---

## E. 從雲端還原（4 項）

> 模擬「換手機」的情境

- [ ] 用 DevTools 清掉 `localStorage.ledger_v1`（不要清 ledger_sync_v1）
- [ ] 重新整理 → 應該看到空帳本但同步設定還在
- [ ] 設定 → 從雲端還原 → 輸入「還原」→ 確認
- [ ] toast 顯示還原筆數，明細頁出現所有紀錄、統計頁餘額正確

---

## F. Cloudflare 部署（4 項）

- [ ] `https://ledger-xxx.pages.dev` 可開啟
- [ ] DevTools → Application → Service Workers → 看到 `service-worker.js` activated
- [ ] DevTools → Application → Manifest → 看到 `記帳本`、`記帳`、icon-192/512 都正確
- [ ] 切 DevTools Offline → 重新整理頁面 → 介面仍可載入（SW 快取生效）

---

## G. iPhone 實機（5 項，需要 iPhone）

- [ ] Safari 開啟 Cloudflare URL → 加入主畫面命名「記帳」
- [ ] 主畫面圖示是橘底白「$」（不是 Safari 預設縮圖）
- [ ] 從主畫面打開 → **無 Safari 網址列**（全螢幕）
- [ ] 點數字鍵盤 → **不會彈出 iOS 系統鍵盤**
- [ ] 設定雲端同步 → 從雲端還原 → 拉到電腦那邊記的資料

---

## H. 設定頁其他控制（3 項）

- [ ] 「立即同步」按鈕在 queue=0 時 disabled
- [ ] 「全部上傳」按鈕在未設定同步時 disabled
- [ ] 失敗紀錄出現時可檢視原因 + 清空

---

## 結果總表

| 項目 | 通過 / 總數 |
|---|---|
| A 設定流程 | / 5 |
| B 自動同步 | / 5 |
| C 離線重試 | / 3 |
| D 首次全量上傳 | / 3 |
| E 從雲端還原 | / 4 |
| F Cloudflare 部署 | / 4 |
| G iPhone 實機 | / 5 |
| H 設定頁控制 | / 3 |

**全綠後**：把 Cloudflare URL 加到主畫面，正式啟用，可進 Phase 4。

---

## 常見錯誤

| 症狀 | 原因 | 解法 |
|---|---|---|
| badge 一直紅 ✕ | URL/Secret 錯 | 設定頁重填 |
| Sheets 沒同步 | Apps Script 部署版本太舊 | 回 Apps Script → 管理部署 → 編輯 → 新版本 |
| `duplicate id` | 已存在同 id | 通常無需處理（已被當失敗略過） |
| 還原後類別顯示「其他」 | 雲端類別中文名找不到對應 | 手動編輯該筆改正確分類 |
| 部署後 PWA 跑舊版 | Service Worker 卡快取 | 改 SW 內 `CACHE_NAME` 版號重新部署 |
